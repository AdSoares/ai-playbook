{
  "tools": [
    {
      "name": "web_search",
      "description": "Searches the web and returns relevant results.",
      "input_schema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "The search query."
          }
        },
        "required": ["query"]
      }
    },
    {
      "name": "fetch_url",
      "description": "Downloads a page and extracts its main text.",
      "input_schema": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "Absolute URL of the page to read."
          }
        },
        "required": ["url"]
      }
    }
  ]
}
